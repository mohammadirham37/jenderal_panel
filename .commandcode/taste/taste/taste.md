# Taste
- Communicates in Indonesian (Bahasa Indonesia); prefers replies in Indonesian. Confidence: 0.85
- Wants errors surfaced explicitly (shown in UI/response) rather than silently swallowed — when something fails with no visible reason, ask for the actual error to be displayed. Explicitly complained about a generic "an internal error occurred" 500: API error responses should carry the actual underlying cause (`err.Error()`), not a placeholder; panic-recovery can stay generic with details logged server-side. Confidence: 0.8
- Prefers committing and pushing changes directly to `main` (no PR/feature-branch flow); repeatedly asked to "push ke main" after each fix is completed, and expects the push to happen immediately once work is validated — confirmed after the npm/timezone fix batch, with a bare "push" after the NODE_VERSION fix batch, and again with "push ke main" after the Laravel deploy-layout fix. Confidence: 0.97
- When the same feature UI is needed in more than one place (e.g., a detail-page tab and a sub-route), extract it into a reusable Svelte component under `web/src/lib/components/` instead of duplicating markup/logic. Confidence: 0.7
- Safe-change workflow: before refactoring an existing page, read the tests that cover it so expectations aren't broken; after changes, validate before reporting the work as done — Go: `go build ./...` plus tests for the affected packages (e.g. `go test ./internal/website/...`); frontend: `npm test` and `npx svelte-check`. Confidence: 0.75
- Prefers root-cause fixes over symptom patches; when reporting a fix, name the actual underlying cause in the summary (e.g., missing service wiring in main.go). Confidence: 0.7
- When the panel injects defaults into a command's environment (e.g. `NODE_VERSION` for the site's `nvm-exec`), project-level configuration takes precedence: only inject the website's NVM default alias when the project has no `.nvmrc`, so a project's own version pin is never overridden by panel defaults. Confidence: 0.55
- Prefers tidy, compact UI layouts: content cards arranged in a responsive two-column grid, two per row (e.g. `grid grid-cols-1 md:grid-cols-2 gap-6 items-start`), instead of stacked full-width cards, keeping related cards side by side (asked for the Laravel .env card to share a row with the composer card "agar desainnya lebih rapih"). Confidence: 0.7
- Server-side file operations on websites follow least privilege and defense in depth: write via sudo as the site's web user with restrictive permissions (`install -o <user> -g <user> -m 600`), enforce input size limits (e.g., 256 KB), locate project directories by detecting marker files (composer.json / artisan / package.json / .env.example) instead of hard-coded paths, and gate new endpoints behind existing RBAC permissions (`websites.view` for reads, `websites.update` for writes). Confidence: 0.7
- Keeps per-website management on the website's detail page rather than on global/aggregate pages: explicitly asked to move the "Website runtimes" section from the Node.js page into the website detail page (Overview tab), so per-site runtime install/version selection lives with the site while the global page keeps only global concerns. Confidence: 0.6
- Prefers constrained form values to be chosen from a dropdown/select of valid options instead of free-text input: asked for the server page's timezone edit to become a `<select>` of valid timezones (full IANA list via `Intl.supportedValuesOf` with a fallback list, and the current value always included as an option) so invalid values/typos are impossible. Confidence: 0.6
- When a website problem needs remediation, prefers a self-service one-click repair button in the panel UI over manual terminal instructions — after being handed manual `mv`-based terminal steps for the misplaced-Laravel-repo 403, immediately asked to "tambahkan tombol untuk fix repair agar direktorinya benar", i.e. add a "Fix Directory Layout" button on the website's Deployment tab (framework-gated, with confirm dialog) so the same fix is available to any user without SSH. Confidence: 0.8
